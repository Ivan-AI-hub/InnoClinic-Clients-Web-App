@page "/doctors/home"
@inherits CancellableComponent
@attribute [Authorize(Roles = "Doctor")]
@if (Doctor != null)
{
    <DoctorCard Doctor="@Doctor" />
    <Row>
        <Left>

            <input type="button" class="btn btn-outline-info w-100" value=@_localizer.GetString(LocalizationType.EditButtonValue) @onclick="@NavigateToEditPage" />
        </Left>
        <Center>
            <ChangeDoctorStatus Doctor="@Doctor" OnStatusChanged="@Reload" />
        </Center>
        <Right>
            <input type="button" class="btn btn-outline-info w-100" value=@_localizer.GetString(LocalizationType.Schedule) @onclick="@NavigateToSchedulePage" />
        </Right>
    </Row>

}
else
{
    <Loading />
}