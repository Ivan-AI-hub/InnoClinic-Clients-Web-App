@page "/receptionists/create"
@attribute [Authorize(Roles = "Admin")]
@inherits CancellableComponent


<div>
    <p class="text-danger">@ErrorMessage</p>
</div>

<EditForm Model="@Data" OnValidSubmit="CreateAsync" class="form">
    <FluentValidationValidator DisableAssemblyScanning=true />
    <ValidationSummary />
    <div class="row">
        <div class="col p-2">
            <FormInputText Label="Имя" Id="firstName" @bind-Value="@Data.FirstName" />
            <FormInputText Label="Фамилия" Id="lastName" @bind-Value="@Data.LastName" />
            <FormInputText Label="Отчество" Id="middleName" @bind-Value="@Data.MiddleName" />
            <FormInputSelect Label="Офис" Id="office" @bind-Value="Data.OfficeId" Values="Offices" GetLabelHandler="@(x => x.Address)" GetValueHandler="@(x => x.Id)" />
            <FormInputDate Label="День рождения" Id="birthDay" @bind-Value="@Data.BirthDay" />
            <SelectImage Title="Аватарка" @bind-Value="@Data.Picture" />
        </div>
    </div>
    <FormSubmitButton @ref="@SubmitButton" ButtonValue="Save Changes" />
</EditForm>
